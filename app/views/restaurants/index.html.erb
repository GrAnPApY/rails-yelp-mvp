<h1>Liste de tout les restaurants<h1>


<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant.id)  %>
    <li>
  <% end %>
</ul>

<%= link_to "Add a restaurant", new_restaurant_path  %>